steps 
-----------
==> express
==> nodemon ==> to avoid restart server manually after every change 
+ adding scripts in package.json 

==> body-parser (app.js) 
to parse incoming request body 
==> multer (app.js & controller)
to handle multipart/form-data requests (file upload)
# file storage config (destination & filename)
# fileFilter (to prevent uploading of certain file types)

==> adding routes (using express router)
- create routes folder
- define routes (and connect it with logic in controller)
- register these routes in app.js

==> controller 
holding the logic for each request

==> express validator 
implement validation for incoming request data in node js apps 
### used inside 
-> routes (import body to validate incoming request)
-> controllers (using validationResult function to validate data)
 - to validate data before processing it
 - to return error message if validation fails

==> database (nosql --> mongodb --> mongoose )
- connect to db using connection url if successful connection ==> run server 
- create models (schema) to define structure of data in db
- use models to interact with db (CRUD operations)

# save 
create & update (findById + save)
# findById
# update
# delete
# find

==> models folder 
to define structure of data in db (schema)

==> in app.js
- set up static serving of my images folder using built-in express middleware --> express.static()
- setting up a general error handling middleware to catch and handle any error that occurs in the app
after all routes
